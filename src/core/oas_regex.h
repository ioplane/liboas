/**
 * @file oas_regex.h
 * @brief Internal regex backend vtable for pattern validation.
 *
 * Abstraction layer over regex engines. PCRE2 is the default backend;
 * QuickJS libregexp provides optional strict ECMA-262 compliance.
 * All patterns are ECMA-262 (unanchored per JSON Schema).
 */

#ifndef LIBOAS_CORE_OAS_REGEX_H
#define LIBOAS_CORE_OAS_REGEX_H

#include <liboas/oas_regex.h>

#endif /* LIBOAS_CORE_OAS_REGEX_H */
